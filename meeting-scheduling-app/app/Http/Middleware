<?php

namespace App\Http\Middleware;

use Closure;
use Illuminate\Http\Request;

class CheckMeetingTime
{
    public function handle(Request $request, Closure $next)
    {
        // Logic to check if the meeting time is valid
        return $next($request);
    }
}